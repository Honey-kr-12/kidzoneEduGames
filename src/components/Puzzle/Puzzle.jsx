import React, { useRef } from 'react';
import ScrollComponent from '../ScrollComponent/ScrollComponent';

const Puzzle = () => {

  return (
<ScrollComponent title={'Recommended'} />
  );
};

export default Puzzle;
